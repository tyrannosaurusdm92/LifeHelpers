# OurSpace shared life-filtered note

This folder has been revised for William and Jasper together. Use the skills as flexible supports, not tests. The default version is tiny, seated/bed-level when possible, MCAS/respiratory-aware, trauma-informed, non-shaming, and caregiver-capacity-aware. Partial completion counts.

Shared folder category: `99_source_manifests_readmes_and_reports`

---

# ADHD JSON + Fillable HTML Export

Original JSON files are preserved in `json/`.

Fillable HTML companions were added in `html/` for worksheet/planner/menu/scheduler/checklist-style JSON sources.

Open `html/index.html` to browse the created forms.

Each HTML file includes:

- a **Print** button
- a link back to the original JSON
- local browser auto-save for typed entries
- print-friendly styling

Created 10 HTML files from 22 JSON files.

See `html_generation_manifest.json` for the created/skipped file list and reasons.
