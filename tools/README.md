# Tools

`install-fonts-from-zips.ps1` is an optional local helper for Windows. It does not include or download fonts. Put your downloaded font ZIPs next to the script, run it, and it will copy the expected `.ttf` files into `assets/fonts/`.

Expected ZIP filenames:

- `magic-school.zip`
- `morris-roman.zip`
