# Movement donor snapshot

`Onyx_Cat_Only_dossier_documented_5_viewer.js` is an exact copy of the user-supplied older `viewer.js` used only as a movement reference for this repair-the-repair pass.

The current runtime does **not** replace itself with this older viewer. It selectively recovers its movement characteristics while keeping the newer direct-open startup fix, physics fallback, body/eye/chest corrections, dossier runtime, backend bridge, inspection UI, and phased feline jump.
